import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.smatexpense',
  appName: 'Smat Expense Tracker',
  webDir: 'out'
};

export default config;
